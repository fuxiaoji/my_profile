# 服务器部署指南

这份指南用于把当前 React + Vite 个人网站部署到服务器。当前改动仅在本地测试，未上传 GitHub。

## 1. 本地构建

在项目根目录执行：

```bash
npm install
npm run build
```

构建完成后会生成 `dist/` 目录，服务器只需要部署这个目录里的静态文件。

本地预览：

```bash
npm run preview
```

默认访问：

```text
http://localhost:4173
```

## 2. 上传到服务器

把本地 `dist/` 目录上传到服务器，例如：

```bash
scp -r dist/* user@server:/var/www/my_profile/
```

也可以用宝塔、1Panel、SFTP 等方式上传，只要最终服务器网站根目录指向构建后的静态文件即可。

## 3. Nginx 配置示例

React Router 使用前端路由，因此需要把不存在的路径回退到 `index.html`。

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    root /var/www/my_profile;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|webp|avif|ico|pdf|mp4|webm)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
```

修改后检查并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 4. HTTPS

推荐使用 Certbot：

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

如果使用宝塔、1Panel 或云厂商面板，也可以直接在面板里申请 Let’s Encrypt 证书。

## 5. 文章、图片、PDF 与简历文件

- `public/` 内的资源会被直接复制到 `dist/`，适合放简历 PDF、图片、视频等公开资源。
- `文章/`、`notes.json`、`articles.json` 等内容如果被代码引用，需要确保构建后对应资源路径可访问。
- 当前本地测试加入了桌面 `note` 文件夹读取能力，这是 Vite 开发服务器里的本地调试功能，不适合直接用于线上静态部署。
- 如果要让笔记在线可访问，需要把要公开的笔记 PDF / Markdown 放进 `public/` 或构建脚本生成的公开目录，再重新构建。

## 6. 常用部署流程

```bash
git pull
npm install
npm run build
rm -rf /var/www/my_profile/*
cp -r dist/* /var/www/my_profile/
sudo nginx -t
sudo systemctl reload nginx
```

如果不想在服务器上安装 Node，也可以只在本地执行 `npm run build`，然后上传 `dist/`。

## 7. 上线前检查清单

- 首页、项目、游戏、文章、笔记、关于页面都能打开。
- `/writing/:id`、`/study/view?...` 等详情页刷新后不 404。
- 三份简历下载按钮可用。
- 邮箱链接可点击。
- 文章图片正常显示。
- 视频资源体积不要过大，建议转成 `.mp4` 或 `.webm`，并开启懒加载或仅在视口内播放。
- 移动端首屏标题没有被裁切。

